\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{float}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{makecell}
\usepackage{microtype}
\usepackage{multirow}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

% TABLES
% discharge protocol distribution/temperature (with soh ranges) replace in table 1

% DIAGRAMS
% actual model architecture diagram instead of placeholder
% finetune diagram (frozen layers) (space) (needed)

% EXPLANATION
% for preprocessing pipeline, perhaps parapgraph is enough honestly, but also have justifications
% feature scaling definitely add a justification that dataset has wide temperature bands, so z score may compress them, there are many papers out there
% compress oxford validation SoH labelling that you used coloumb counting, much shorter
% mention shuffling mechanics (to break symmetry) 
% mention pytorch and gpu
% maybe mention the coding stack
% abstract
% references

\begin{document}

\title{State of Health Estimation Using a Novel Dataset Under Extreme Temperature and Aging Conditions}

\author{\IEEEauthorblockN{1\textsuperscript{st} Sahar Qaadan}
	\IEEEauthorblockA{\textit{Department of Mechatronics Engineering} \\
		\textit{German Jordanian University}\\
		Madaba JO-11180, Jordan \\
		sahar.qadan@gju.edu.jo}
	\and
	\IEEEauthorblockN{2\textsuperscript{nd} Yousef Alfoqaha}
	\IEEEauthorblockA{\textit{Department of Computer Engineering} \\
		\textit{German Jordanian University}\\
		Madaba JO-11180, Jordan \\
		y.mustafa1@gju.edu.jo}
}

\maketitle

\begin{abstract}
	WIP
\end{abstract}

\begin{IEEEkeywords}
	State of Health Estimation, Machine Learning, Lithium-Ion Batteries, Benchmark Dataset
\end{IEEEkeywords}

\section{Introduction}
With the electric vehicle (EV) market share surpassing 20\% of new global car sales, battery management systems (BMS) in EVs rely on accurate state of health (SoH) estimations to ensure a margin of safety in maintaining critical vehicle components. While SoH, in the capacity sense, is defined as $\mathrm{SOH}_C = C / C_{\mathrm{init}}$, microscopic manufacturing variations cause each lithium ion battery (LiB) to follow a highly non-linear degradation trajectory; this makes tracking SoH a non-trivial task, especially in realistic driving scenarios. Data-driven approaches typically employ diagnostic frameworks for real-time EV monitoring; by extracting instant features from available charge-discharge windows, they dynamically track sudden non-linear aging mechanisms like solid electrolyte interphase (SEI) layer growth and lithium plating as they occur.

Recent diagnostic models combine convolutional neural networks (CNN) for spatial feature extraction with recurrent neural networks (RNN), such as Long Short-Term Memory (LSTM) or Gated Recurrent Units (GRU), to model temporal dependencies common in LiB degredation. These networks frequently integrate attention mechanisms to weight cycle data and utilize support vector machines (SVM) for final SoH regression. Other studies incorporate preprocessing techniques like Whale Optimization Algorithm-Variational Modal Decomposition (WOA-VMD) to filter cyclic noise, or explore neural ordinary differential equations (NODE) to model continuous capacity decay.

Because generating battery degradation data is a laborious and time-consuming process, the vast majority of these models are trained and benchmarked on open-datasets such as NASA Ames, Oxford, CALCE, TJU, and HUST. While these datasets offer varied cell chemistries, they restrict model evaluation to constant-current (CC) charging phases and narrow ambient temperatures.

Thus, to complement existing benchmark datasets, a multi-protocol LiB degradation dataset was developed in collaboration with the University of Wuppertal. This dataset comprises 8 Samsung INR21700-50E LiBs, which were cycled across a wide temperature spectrum (-20°C to 45°C) using a combination of realistic operational protocols, including the Worldwide Harmonised Light Vehicles Test Cycle (WLTC), Hybrid Pulse Power Characterization (HPPC), pulse, and constant-current segments.

To establish a benchmark baseline, evaluation was performed on a diagnostic estimator combining CNN, BiGRU, and attention pooling layers. We evaluate this baseline across the dataset's temperature bands and protocols, and verify its cross-dataset transferability by fine-tuning it on the Oxford Battery Degradation Dataset. A comparative summary of public battery datasets and estimation frameworks is provided in Table \ref{tab:related_works}.

\begin{table*}[htbp]
	\caption{Comparison of Benchmark Battery Degradation Datasets and Diagnostic Estimation Frameworks}
	\label{tab:related_works}
	\centering
	\small
	\begin{tabular}{l l c c l}
		\hline
		\textbf{Dataset / Study} & \textbf{Cell Form / Chemistry}          & \textbf{Temperature Range} & \textbf{Discharge Protocols} & \textbf{Baseline Evaluated} \\
		\hline
		NASA Ames                & \makecell[l]{18650 Cylindrical                                                                                                    \\ LCO-NCA} & $20^\circ\mathrm{C}$ to $30^\circ\mathrm{C}$ & CC & SVR, LSTM, ANODE \\
		\hline
		Oxford Dataset 1         & \makecell[l]{Pouch / LCO                                                                                                          \\ ($740\,\mathrm{mAh}$)} & $40^\circ\mathrm{C}$ (Static) & CC & Dual-Attn Graph CNN \\
		\hline
		HUST Dataset             & \makecell[l]{18650 Cylindrical                                                                                                    \\ LFP} & Uncontrolled Ambient & Multi-step CC & Deep Transfer Learning \\
		\hline
		\textbf{This Work}       & \makecell[l]{\textbf{21700 Cylindrical}                                                                                           \\ \textbf{NMC ($5.0\,\mathrm{Ah}$)}} & \textbf{$-20^\circ\mathrm{C}$ to $45^\circ\mathrm{C}$} & \textbf{WLTC, HPPC, Pulse, CC} & \textbf{CNN+BiGRU+Attn} \\
		\hline
	\end{tabular}
\end{table*}

\section{Data Processing}

\subsection{Dataset}
The dataset was obtained from the Scienlab SL1007A charge-discharge testing platform, where eight Samsung INR21700-50E lithium-ion cells were tested from fresh SoH levels to beyond EOL. Telemetry of each cell was recorded using Measurement and Control Units (MCU1--MCU8) and exported using the ASAM Measurement Data Format (MF4). Cell specifications are given in Table \ref{tab:cell_specs}.

\begin{table}[H]
	\caption{CELL SPECIFICATIONS}
	\label{tab:cell_specs}
	\begin{center}
		\begin{tabular}{lc}
			\hline
			\textbf{Parameter}                       & \textbf{Value}                   \\
			\hline
			Nominal capacity ($C_{\text{nom}}$)      & $5.0\,\text{Ah}$                 \\
			Upper cut-off voltage ($U_{\text{max}}$) & $4.2\,\text{V}$                  \\
			Lower cut-off voltage ($U_{\text{min}}$) & $2.5\,\text{V}$                  \\
			Max. discharge current                   & $-20\,\text{A}$ ($-4.0\text{C}$) \\
			Max. charge current                      & $8\,\text{A}$ ($2.0\text{C}$)    \\
			Temperature range                        & $-20$ to $60\,^{\circ}\text{C}$  \\
			State of charge range                    & $0$ to $100\,\%$                 \\
			\hline
		\end{tabular}
	\end{center}
\end{table}

Initial measurements were taken across constant, pulse, HPPC, and WLTC protocols (Fig. \ref{fig:protocols}), with ambient temperatures at -20, -10, -5, 0, 10, 25, and $40^{\circ}\text{C}$. Constant and pulse protocols were tested at 0.1, 0.5, 1.0, and 2.0C. Cyclic aging was then performed using constant discharge at 1.0C and $25^{\circ}\text{C}$. Post aging measurements were taken using the same procedure as the initial phase (excluding WLTC). Exact distributions are shown in Table \ref{tab:temp_phase_matrix}.

\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{discharge_protocols.pdf}}
	\caption{Discharge profiles for the four test protocols: Constant, Pulse, HPPC, and WLTC.}
	\label{fig:protocols}
\end{figure}

\input{temp_distribution.tex}

\subsection{Pre-processing Pipeline}
As per the dataset engineer's advice, voltage (V), current (A), temperature (T), and ambient temperature ($T_{amb}$) are primary channels recorded from the test platform's sensors.
The components of the pre-processing pipeline are as follows:

\begin{enumerate}
	\item \textbf{Extract discharge cycles:} Using discharge start/end signals provided by the testing platform to filter only discharge cycles.
	\item \textbf{Resampling:} Raw signals recorded by the test platform at up to $1\text{~kHz}$ ($1\text{~ms}$) were downsampled to a uniform $1\text{~Hz}$ ($1\text{~s}$). This eliminates sample-rate discrepancies across asynchronous channels in the raw MF4 files.
	\item \textbf{Hierarchical Data Format (HDF5):} Converting each discharge cycle away from MF4 to HDF5 allows for O(1) disk reads when loading the dataset, while also only taking primary channels (V, I, T, $T_{amb}$) to reduce memory usage.
	\item \textbf{Ambient temperature:} $T_{amb}$ is controlled by the test platform at a fixed value for each discharge cycle, thus an average value was taken and stored as an HDF5 attribute.
	\item \textbf{SoH labelling:} Raw SoH is calculated for each discharge cycle and written to as an HDF5 attribute.
\end{enumerate}

\subsection{SoH Labeling}
Raw SoH for each discharge cycle was computed via Coulomb counting:

\begin{equation}
	\text{SoH}_{\text{raw}} = \frac{\int_{t_{\text{start}}}^{t_{\text{end}}} |I(t)| \, dt}{C_{\text{nom}}}
\end{equation}

Measured capacity depends on discharge rate and $T_{\text{amb}}$, so raw SoH is not directly comparable across cycles. Discharge cycles at $1\text{C}$ and $25^\circ\text{C}$, the only condition common to all phases (Table \ref{tab:phase_soh_summary}), are used as reference points. A 4th degree polynomial is fit over cycle index to these reference points, giving a per-MCU SoH trajectory (Fig. \ref{fig:soh_trajectories}). Reference-point SoH ranges and cycle counts are given in Table \ref{tab:mcu_soh_summary}.


\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{soh_trajectories.pdf}}
	\caption{Fitted SoH trajectories for all eight MCUs across their full discharge cycle histories.}
	\label{fig:soh_trajectories}
\end{figure}

\input{mcu_soh_summary.tex}

\subsection{Feature Scaling}

Z-score standardization is sensitive to the dataset's extreme temperature and aging conditions, where wide value swings are not considered outliers. Robust min-max scaling is used instead for training stability, bounded by the $1^{\text{st}}$ and $99^{\text{th}}$ percentiles ($p_{01}$, $p_{99}$) of each channel across all 14,262,367 time steps. Percentile bounds per feature are given in Table \ref{tab:feature_stats}.

\begin{equation}
	x_{\text{norm}} = \frac{x - p_{01}}{p_{99} - p_{01}}
\end{equation}

\input{feature_stats.tex}

\section{Methodology}

\subsection{Problem Formulation}
Given a sequence of $V$, $I$, and $T$ over a discharge cycle, conditioned on $T_{\text{amb}}$, an estimator function $f(\cdot)$ predicts the cell's $\text{SoH}$:

\begin{equation}
	\widehat{\text{SoH}} = f(V, I, T \mid T_{\text{amb}})
\end{equation}

\subsection{Estimator Architecture}

A window of sequences $V$, $I$, and $T$ of length $N$ pass through a stem 1D Convolutional Neural Network (1D-CNN) layer followed by two strided 1D-CNN blocks ($2\times$, $4\times$ base channels; GroupNorm, LeakyReLU, dropout), followed by Feature-wise Linear Modulation (FiLM) conditioning on $T_{\text{amb}}$:

\begin{equation}
	\text{FiLM}(h) = (1 + \gamma(T_{\text{amb}})) \odot h + \beta(T_{\text{amb}})
\end{equation}

where $\gamma$ and $\beta$ are learned affine parameters. The modulated features pass through a 2-layer bidirectional Gated Recurrent Unit (BiGRU). An additive attention mechanism then pools over the GRU output timesteps:

\begin{equation}
	\alpha_t = \frac{\exp(w^\top \tanh(W h_t))}{\sum_{\tau} \exp(w^\top \tanh(W h_\tau))}, \quad z = \sum_t \alpha_t h_t
\end{equation}

where $h_t$ is the BiGRU output at timestep $t$. The pooled representation $z$ is projected to a scalar SoH estimate.

\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{architecture.pdf}}
	\caption{Model architecture: 1D-CNN blocks, BiGRU, and FiLM conditioning on $T_{\text{amb}}$.}
	\label{fig:architecture}
\end{figure}


\subsection{Training Setup}

Raw input sequences (1,500 to 30,000 timesteps per cycle) were sliced into 1,500-timestep windows (15 minutes) with a 1,000-timestep stride. The model predicted the SoH for each window, and these predictions were averaged to produce a single SoH value per cycle.

% \begin{figure}[H]
% 	\centerline{\includegraphics[width=\columnwidth]{window_splitting.pdf}}
% 	\caption{Window-splitting logic during training.}
% 	\label{fig:window_slicing}
% \end{figure}

The eight MCUs were split 6/1/1 into training, validation, and test sets. MCUs 2 and 7 were held out for validation and testing, respectively, as their SoH range ($95.6$--$67.5\%$) falls within the training range ($97.4$--$42.3\%$), evaluating interpolation rather than extrapolation.

In total, the training set comprised 1,071 cycles (7,227 windows), while the validation set contained 185 cycles (1,341 windows). All input sequences and conditions were normalized using robust min-max scaling. The Estimator's hyperparameters are given in Table \ref{tab:estimator_hparams}.

\input{hyperparameters.tex}

The model was trained using the AdamW optimizer (weight decay of $10^{-2}$) and a batch size of 128 windows to minimize the mean squared error (MSE) loss. The \texttt{ReduceLROnPlateau} scheduler was used on an initial learning rate of $2 \times 10^{-2}$, which decayed by a factor of $0.5$ (minimum $10^{-5}$) upon encountering a 15-epoch plateau in validation loss. Early stopping was used to halt training after 50 epochs without improvement, restoring the model weights that achieved the lowest validation loss.

\subsection{Results}
Evaluation was performed on the testing MCU across phases (Table \ref{tab:baseline_results}) and across temperature bands and discharge protocols (Table \ref{tab:temp_protocol_results}).

\input{baseline_results.tex}

Most overall metrics derive from the aging phase, which achieves low error. The initial phase reaches $1.2\%$ error (RMSE $0.016$). Aging is the strongest phase ($R^2 = 0.89$, $1.2\%$ error, RMSE $0.013$).

Notably, Initial and Aging achieve similar metrics despite the former having far fewer cycles and greater protocol/temperature variance. Post-Aging error rises to $3.8\%$ (RMSE $0.034$).  Overall, $R^2 = 0.020$ and $1.8\%$ error across $62.8$--$96.2\%$ SoH.

\input{temp_protocol_results.tex}

The $40^{\circ}\text{C}$ band achieves the highest fit among temperature slices ($R^2 = 0.96$, $1.9\%$ error), while $-20^{\circ}\text{C}$ trails slightly ($R^2 = 0.94$, $2.7\%$ error) and $-10^{\circ}\text{C}$ shows the lowest fit among the higher-cycle bands ($R^2 = 0.86$, $4.3\%$ error).

Within each temperature band, the estimator performs consistently across the SoH spectrum. $-20^{\circ}\text{C}$ spans $68.1$--$93.7\%$ at $R^2 = 0.94$ and $40^{\circ}\text{C}$ spans $66.8$--$95.1\%$ at $R^2 = 0.96$.

Across discharge protocols, (performance metrics) with each protocol spanning multiple temperature bands (Table~\ref{tab:phase_soh_summary}). Constant and HPPC achieve the highest fits ($R^2 = 0.92$ and $0.99$), and Pulse follows ($R^2 = 0.95$). WLTC has an $R^2$ of -0.11, but this reflects $3$ cycles restricted to the Initial phase, giving a narrow SoH band ($92.1$--$95.3\%$) (Table~\ref{tab:phase_soh_summary}).

The testing MCU's SoH trajectory with predicted points is shown in Fig.~\ref{fig:test_trajectory}.

\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{test_trajectory.pdf}}
	\caption{Testing MCU SoH trajectory with predicted SoH points.}
	\label{fig:test_trajectory}
\end{figure}

\section{Cross-Dataset Validation}

\subsection{Oxford Battery Degredation Dataset 1}

The model was fine-tuned on the Oxford Battery Degradation Dataset 1 to measure cross-dataset performance. The dataset comprises eight Kokam $740\,\text{mAh}$ pouch cells (SLPB526495), presenting a significant distribution shift from the Samsung INR21700-50E cylindrical cells ($5.0\,\text{Ah}$) used for primary training. The Oxford dataset cell specifications are listen in Table~\ref{tab:oxford_cell_specs}.

\begin{table}[H]
	\caption{OXFORD CELL SPECIFICATIONS}
	\label{tab:oxford_cell_specs}
	\begin{center}
		\begin{tabular}{lc}
			\hline
			\textbf{Parameter}                       & \textbf{Value}                   \\
			\hline
			Cell model                               & Kokam SLPB526495                 \\
			Form factor / Chemistry                  & Pouch / LCO                      \\
			Nominal capacity ($C_{\text{nom}}$)      & $740\,\text{mAh}$                \\
			Upper cut-off voltage ($U_{\text{max}}$) & $4.2\,\text{V}$                  \\
			Lower cut-off voltage ($U_{\text{min}}$) & $2.7\,\text{V}$                  \\
			Max. discharge current                   & $1.48\,\text{A}$ ($2.0\text{C}$) \\
			Max. charge current                      & $0.74\,\text{A}$ ($1.0\text{C}$) \\
			Testing temperature                      & $40\,^{\circ}\text{C}$           \\
			State of charge range                    & $0$ to $100\,\%$                 \\
			\hline
		\end{tabular}
	\end{center}
\end{table}

SoH labels were computed via Coulomb counting (1). SoH ranges for each cell are given in Table \ref{tab:oxford_soh_summary}.

\begin{table}[H]
	\caption{Oxford Cell SoH Range and Cycle Count}
	\label{tab:oxford_soh_summary}
	\begin{center}
		\footnotesize
		\begin{tabular}{lcc}
			\hline
			\textbf{Cell} & \textbf{SoH Range (\%)} & \textbf{Cycles} \\
			\hline
			Cell 1        & $100.0$--$82.1$         & 82              \\
			Cell 2        & $100.0$--$81.4$         & 82              \\
			Cell 3        & $100.0$--$83.5$         & 82              \\
			Cell 4        & $100.0$--$82.9$         & 82              \\
			Cell 5        & $100.0$--$84.1$         & 82              \\
			Cell 6        & $100.0$--$83.0$         & 82              \\
			Cell 7        & $100.0$--$82.5$         & 82              \\
			Cell 8        & $100.0$--$81.9$         & 82              \\
			\hline
		\end{tabular}
	\end{center}
\end{table}

\subsection{Fine-Tuning Setup}

The 1D-CNN, FiLM, and BiGRU layers were frozen, leaving only the attention pooling mechanism and output regression head open for fine-tuning. The eight cells were partitioned into a 6/1/1 split, allocating Cells 1--6 for fine-tuning, Cell 7 for validation, and Cell 8 for testing (Table~\ref{tab:oxford_soh_summary}). Feature scaling bounds and hyperparameter settings were kept identical to the primary training setup.

\subsection{Results}

Evaluating the model in a zero-shot setting yields severe performance degradation, resulting in an RMSE of $0.406$ ($46.8\%$ error) and an $R^2$ of $-36.66$ (Table~\ref{tab:oxford_results}).

However, unfreezing only the attention pooling mechanism and output regression head resolves this domain discrepancy. Upon fine-tuning, performance improves dramatically, achieving an RMSE of $0.008$, an MAE of $0.006$, an $R^2$ of $0.99$, and reducing the mean percentage error to $0.8\%$ across all 76 evaluation cycles.

\input{oxford_results.tex}

As shown in Fig.~\ref{fig:oxford_trajectory}, in contrast to the zero-shot predictions, the fine-tuned predictions closely track ground-truth capacity fade.
\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{oxford_trajectory.pdf}}
	\caption{Estimated versus ground-truth SoH trajectory on the testing cell for both zero-shot and fine-tuned scenarios.}
	\label{fig:oxford_trajectory}
\end{figure}


\section{Feature Interpretability}

\subsection{Permutation Feature Importance (PFI)}
PFI was used to measure which inputs the model relies on for its predictions. PFI shuffles one feature at a time across discharge cycles and measures the resulting drop in performance:
\begin{equation}
	\Delta \text{RMSE}_{j} = \text{RMSE}_{\text{perm}(j)} - \text{RMSE}_{\text{base}}, \quad j \in \{I, V, T, T_{amb}\}
\end{equation}

Shuffling was only done within the following controlled testing conditions: discharge protocol, ambient temperature, and discharge rate. This isolates the true importance of each feature from differences caused by the test setup itself.

\subsection{Results}
The stratified PFI results (Table~\ref{tab:pfi_results} and Fig.~\ref{fig:pfi_importance}) demonstrate that current ($I$) and voltage ($V$) are the primary drivers of model predictions across all protocols.

\input{pfi_results.tex}

\begin{figure}[H]
	\centerline{\includegraphics[width=\columnwidth]{pfi_importance.pdf}}
	\caption{Protocol-stratified Permutation Feature Importance ($\Delta\text{RMSE}$) across input channels ($I, V, T, T_{\text{amb}}$).}
	\label{fig:pfi_importance}
\end{figure}

\section{Discussion}
The aging phase is the strongest, likely due to limited variance in discharge protocol (1C discharge at 25\textdegree C). Its constant 1C discharge yields a consistent waveform the CNN/BiGRU exploits, while still tracking declining SoH across discharges.

Degradation capture holds across the full SoH range within a given temperature, not only at its extremes.

likely because MCU SoH trajectories diverge after accelerated aging (Fig.~\ref{fig:soh_trajectories}), producing a less-represented distribution in training.

R2 is negative for the initial phase, this is likely due to the phase's narrow SoH range ($91.8$--$96.2\%$): with little target variance, $R^2$ amplifies residual error and becomes uninformative. Therefore, RMSE and \%Err are the meaningful metrics here.

Initial and aging are similar in performance, suggesting the model generalizes well across both phases despite varying temperatures.

Notably, $-20^{\circ}\text{C}$ is $45^{\circ}\text{C}$ from the $25^{\circ}\text{C}$ baseline, versus $15^{\circ}\text{C}$ for $40^{\circ}\text{C}$, so its $R^2 = 0.94$ reflects generalization across a larger temperature shift than the $40^{\circ}\text{C}$ result.

This offset is expected due to the significant domain shift across nominal capacity ($5.0\,\text{Ah}$ to $740\,\text{mAh}$), cathode chemistry (NMC to LCO), and cell form factor (cylindrical to pouch).

While neural network architectures obscure deterministic algebraic extraction, the model's heavy reliance on $V$ and $I$ aligns directly with fundamental degradation mechanics. As State of Health (SoH) decreases, solid-electrolyte interphase (SEI) growth increases internal resistance, intrinsically altering the voltage response to a given current load. The model's prioritization of these features suggests it successfully leverages these underlying electro-chemical markers rather than dataset artifacts.

Furthermore, feature reliance shifts dynamically based on thermal dissipation. Under Constant discharge, sustained continuous loads cause significant heat accumulation, leading the model to additionally utilize surface temperature ($T$) ($\Delta\text{RMSE} = +0.0130$). Under dynamic profiles (HPPC and Pulse), rapid current transitions and rest periods allow for localized heat dissipation, reducing the predictive utility of surface thermal signals.

\section{Conclusion}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
